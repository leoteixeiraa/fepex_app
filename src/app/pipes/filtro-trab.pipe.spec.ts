import { FiltroTrabPipe } from './filtro-trab.pipe';

describe('FiltroTrabPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTrabPipe();
    expect(pipe).toBeTruthy();
  });
});
